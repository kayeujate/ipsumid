// Example object
const outer = {
    type: "group",
    // other properties and methods
};

// Conditional statement
if (outer.type === "group") {
    // Code block to execute if outer.type is "group"
    console.log("Type is a group.");
} else {
    // Optional else block
    console.log("Type is not a group.");
}
